# 🚀 دليل البداية السريعة - Q Bot

## الخطوة 1: إنشاء البوت في ديسكورد

1. اذهب إلى: https://discord.com/developers/applications
2. اضغط **New Application**
3. سمّه "Q" أو أي اسم تريد
4. اذهب إلى **Bot** من القائمة الجانبية
5. اضغط **Add Bot**
6. **مهم:** احفظ الـ **Token** (اضغط Reset Token ثم Copy)

---

## الخطوة 2: تفعيل الصلاحيات

في صفحة **Bot**:

### Privileged Gateway Intents (مهم جداً!)
- ✅ فعّل **PRESENCE INTENT**
- ✅ فعّل **SERVER MEMBERS INTENT**  
- ✅ فعّل **MESSAGE CONTENT INTENT**

---

## الخطوة 3: دعوة البوت لسيرفرك

1. اذهب إلى **OAuth2** → **URL Generator**
2. في **SCOPES** اختر:
   - ✅ `bot`
   - ✅ `applications.commands`

3. في **BOT PERMISSIONS** اختر:
   - ✅ View Channels
   - ✅ Send Messages
   - ✅ View Audit Log
   - ✅ Manage Roles
   - ✅ Ban Members
   - ✅ Kick Members
   - ✅ Moderate Members
   - ✅ Read Message History

4. انسخ الرابط من الأسفل وافتحه في المتصفح
5. اختر سيرفرك واضغط **Authorize**

---

## الخطوة 4: معرف الديسكورد حقك وحق السيرفر

### معرف المستخدم (OWNER_ID):
1. في ديسكورد، اذهب **User Settings** → **Advanced**
2. فعّل **Developer Mode**
3. اضغط right-click على اسمك → **Copy ID**

### معرف السيرفر (GUILD_ID):
1. اضغط right-click على أيقونة السيرفر
2. اختر **Copy ID**

---

## الخطوة 5: إعداد ملفات البوت

### للتشغيل المحلي (على جهازك):

1. **ثبت Python** (3.8 أو أحدث): https://python.org
2. **ثبت المتطلبات:**
```bash
pip install -r requirements.txt
```

3. **أنشئ ملف `.env` في نفس المجلد:**
```env
TOKEN=ضع_التوكن_هنا
OWNER_ID=ضع_معرفك_هنا
GUILD_ID=ضع_معرف_السيرفر_هنا
DB_KEY=غير_هذا_لشيء_آمن
```

4. **شغّل البوت:**
```bash
python bot.py
```

---

## الخطوة 6: للنشر على Railway

### إنشاء مشروع Railway:

1. اذهب إلى: https://railway.app
2. سجل دخول بـ GitHub
3. اضغط **New Project** → **Empty Project**
4. اضغط **+ New** → **GitHub Repo** أو **Empty Service**

### إضافة المتغيرات:

1. اذهب **Variables**
2. أضف:
```
TOKEN = توكن_البوت
OWNER_ID = معرفك
GUILD_ID = معرف_السيرفر
DB_KEY = مفتاح_آمن_عشوائي
DM_ALERTS = true
ENCRYPT_DB = true
```

### رفع الملفات:

1. إما ربط GitHub repo
2. أو استخدم Railway CLI:
```bash
railway login
railway link
railway up
```

3. **Start Command** يكون تلقائياً: `python bot.py`

---

## الخطوة 7: اختبار البوت

1. **تأكد البوت أونلاين** في السيرفر
2. **أرسل رسالة خاصة للبوت:**
```
.help
```

3. **لو ما رد:**
   - تأكد الـ `OWNER_ID` صحيح
   - تأكد فتحت الـ DMs للبوت
   - شوف السجل في `q_bot.log`

---

## الخطوة 8: أول أمر

### راقب شخص مشبوه:
```
.راقب 123456789012345678
```

### شوف الفلاتر:
```
.filters
```

### طفي فلتر معين:
```
.filter members off
```

---

## 🎯 نصائح مهمة

### ✅ افعل:
- غيّر `DB_KEY` لشيء قوي وعشوائي
- اضبط الفلاتر حسب حاجتك
- ضيف المشرفين الموثوقين للـ whitelist
- اختبر الأوامر قبل الاعتماد عليها

### ❌ لا تفعل:
- لا تشارك التوكن أبداً
- لا تخلي البوت في سيرفرات ثانية (بيطلع تلقائياً)
- لا تنسى تفعل Developer Mode عشان تنسخ المعرفات

---

## 🆘 مشاكل شائعة

### البوت ما يرد على الأوامر:
- تأكد `OWNER_ID` صحيح
- تأكد ترسل في DM مو في السيرفر
- تأكد الأوامر تبدأ بـ `.`

### البوت ما يشوف الأحداث:
- تأكد فعلت الـ Privileged Intents
- تأكد البوت عنده صلاحية View Audit Log

### قاعدة البيانات تعطي أخطاء:
- احذف `db.json` وخل البوت ينشئه من جديد
- أو طفي التشفير: `ENCRYPT_DB=false`

---

## 📖 المزيد

لمعلومات أكثر تفصيلاً، اقرأ `README.md`

**جاهز للحماية! 🛡️**
